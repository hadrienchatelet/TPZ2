{% extends "base.html.twig" %}
{% block title %}Person{% endblock %}
{% block body %}
    <h1>Index</h1>
    <br/>
    <a href="{{ path("newPerson") }}">New Person</a>
    <br/>
    <a href="{{ path("newMaterial") }}">New Material</a>

    <ul>
        {% for person in persons %}
            <li>nom: {{ person.name }} , max weight: {{ person.maxWeight }} </li>
        {% endfor %}
    </ul>
{% endblock %}
